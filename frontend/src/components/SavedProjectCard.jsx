function SavedProjectCard({ project, onOpen, onDelete, onRename, onFavorite }) {
  const title = project.name || project.result?.title || "Untitled Innovation";
  return <article className="rounded-2xl border border-white/8 bg-slate-900/60 p-5"><div className="flex items-start justify-between gap-3"><div><input aria-label={`Rename ${title}`} value={title} onChange={(e)=>onRename(e.target.value)} className="w-full bg-transparent text-lg font-bold text-cyan-300 outline-none focus:ring-1 focus:ring-cyan-400/30"/><p className="mt-1 text-xs text-slate-600">{project.createdAt} · {project.versions?.length || 1} version{(project.versions?.length || 1)!==1?"s":""}</p></div><button aria-label="Favorite project" onClick={onFavorite} className={`text-xl ${project.favorite ? "text-amber-300" : "text-slate-700"}`}>★</button></div><p className="mt-3 line-clamp-2 text-sm leading-6 text-slate-400">{project.idea}</p><div className="mt-4 grid grid-cols-3 gap-2">{[["Innovation",project.result?.innovation_score],["Novelty",project.result?.novelty_score],["Feasible",project.result?.feasibility_score]].map(([l,v])=><div key={l} className="rounded-lg bg-white/[.035] p-2 text-center"><p className="text-[10px] uppercase tracking-wider text-slate-600">{l}</p><p className="mt-1 font-bold text-white">{v ?? "--"}%</p></div>)}</div><div className="mt-4 flex gap-2"><button onClick={()=>onOpen(project)} className="rounded-lg bg-cyan-400 px-4 py-2 text-xs font-bold text-slate-950">Open</button><button onClick={()=>onDelete(project.id)} className="rounded-lg border border-rose-400/15 px-4 py-2 text-xs font-semibold text-rose-300">Delete</button></div></article>;
}
export default SavedProjectCard;
